services:
  - type: web
    name: rag-assistant
    env: python
    region: oregon
    plan: free
    rootDir: RAG-assistance
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.10"
